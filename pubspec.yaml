name: torino_events
description: Cosa fare a Torino — swipe, calendario, fonti.
publish_to: "none"
version: 0.1.0

environment:
  sdk: ">=3.3.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  http: ^1.2.0
  provider: ^6.1.2
  flutter_card_swiper: ^7.0.1
  table_calendar: ^3.1.2
  flutter_map: ^7.0.2
  latlong2: ^0.9.1
  url_launcher: ^6.3.0
  shared_preferences: ^2.2.3
  flutter_secure_storage: ^9.2.2
  yaml: ^3.1.2
  intl: ^0.19.0

flutter:
  uses-material-design: true
